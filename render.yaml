services:
  - type: web
    runtime: node
    name: zenith-crm
    buildCommand: npm install
    startCommand: NODE_ENV=production node server.js
    envVars:
      - key: NODE_ENV
        value: production
